name: elearn_mobile
description: "eLearn mobile application."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.1.1
  flutter_math_fork: ^0.7.0
  google_fonts: ^6.3.0

  cupertino_icons: ^1.0.8
  go_router: ^17.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
